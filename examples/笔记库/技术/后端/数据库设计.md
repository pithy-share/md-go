# 数据库设计

## 范式

| 范式 | 要求 |
|------|------|
| 1NF | 字段原子性，不可再分 |
| 2NF | 消除部分函数依赖 |
| 3NF | 消除传递函数依赖 |

## 索引

- 为 `WHERE`、`JOIN`、`ORDER BY` 涉及的字段建索引
- 索引提升读、降低写，权衡使用
- 联合索引遵循「最左前缀」原则

## 反范式

读多写少的场景，适当冗余字段以避免多表 JOIN，用空间换时间。

## 设计建议

1. 主键用自增 ID 或 UUID
2. 时间字段统一 UTC
3. 软删除（`deleted_at`）而非物理删除
